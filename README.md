## SCHOOL INVEST TRACKER

# Installation
    - With makefile:
        - run `make`
    - Without makefile:
        - run `docker network create -d bridge sit-network`
        - run `docker compose up -d`